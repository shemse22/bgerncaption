import { GoogleGenAI } from '@google/genai'; import fs from 'node:fs'; const ai = new GoogleGenAI({apiKey: 'AIzaSyA_FAKE_KEY_FOR_TESTING_PURPOSES'}); async function run() { try { const blob = new Blob([Buffer.from('test')], { type: 'text/plain' }); await ai.files.upload({ file: blob, config: { mimeType: 'text/plain', displayName: 'test.txt' } }); console.log('success'); } catch(e) { console.error('Caught error:', e.message); } } run();
